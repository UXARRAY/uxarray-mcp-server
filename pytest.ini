[pytest]
asyncio_mode = auto
testpaths = tests
# tests/test_multi_turn_eval.py imports the eval harness under evals/, which
# is deliberately not shipped as part of the installed package.
pythonpath = .
